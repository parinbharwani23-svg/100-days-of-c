 #include<stdio.h>
 #include<math.h>
 int main()
{ 
  int l,b,area,perimeter;
  printf("Enter length and bredth:\n");
  scanf("%d%d",&l,&b);
  area=(l*b);
  perimeter=2*(l+b);
  printf("area=%d,perimeter=%d",area=l*b,perimeter=2*(l+b));
  return 0;
 }  